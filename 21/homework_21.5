# 5. Модифікувати попередній запит, щоб з отриманого результату вибрати тільки записи, де значення users більше 1
SELECT age, COUNT(*) as users
FROM users
WHERE age > 30
GROUP BY age
HAVING COUNT(*) > 1
ORDER BY users DESC, age ASC;