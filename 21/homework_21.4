# 4. Написати sql запит, що буде робити те саме, що і в завданні 3, але виводити дані відсортовані
# по кількості користувачів у спадаючому порядку та по віку у зростаючому порядку.
SELECT age, COUNT(*) as users
FROM users
WHERE age > 30
GROUP BY age
ORDER BY users DESC, age ASC