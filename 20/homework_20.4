
'CREATE TABLE IF NOT EXISTS users ('
    'id INTEGER PRIMARY KEY,'
    'first_name TEXT NOT NULL,'
    'last_name TEXT NOT NULL,'
    'age INTEGER,
     UNIQUE (first_name, last_name) ON CONFLICT ABORT
    )'
